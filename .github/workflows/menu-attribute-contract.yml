name: Menu Attribute Contract

on:
  push:
    branches: ["main", "agent/complete-crystal-bound-foundation"]
  pull_request:

jobs:
  validate-menu-attributes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate shared menu attributes and close-state cleanup
        shell: python
        run: |
          from pathlib import Path
          root = Path("src")
          files = {
              "dialog": root / "StarterPlayer/StarterPlayerScripts/NPCDialogMenu.client.lua",
              "touch": root / "StarterPlayer/StarterPlayerScripts/TouchMenus.client.lua",
              "bridge": root / "ServerScriptService/NPCMenuBridge.server.lua",
              "quest": root / "StarterPlayer/StarterPlayerScripts/QuestMenu.client.lua",
              "shop": root / "StarterPlayer/StarterPlayerScripts/ShopMenu.client.lua",
              "crystal": root / "StarterPlayer/StarterPlayerScripts/InventoryMenu.client.lua",
              "craft": root / "StarterPlayer/StarterPlayerScripts/CraftingMenu.client.lua",
              "achievement": root / "StarterPlayer/StarterPlayerScripts/AchievementMenu.client.lua",
          }
          text = {key: path.read_text(encoding="utf-8") for key, path in files.items()}
          attributes = [
              "OpenQuestMenu",
              "OpenShopMenu",
              "OpenCrystalMenu",
              "OpenCraftingMenu",
              "OpenAchievementMenu",
              "OpenNPCDialog",
          ]
          problems = []
          for attribute in attributes:
              if attribute not in text["touch"] and attribute != "OpenNPCDialog":
                  problems.append(f"TouchMenus missing {attribute}")
              if attribute == "OpenNPCDialog" and attribute not in text["bridge"]:
                  problems.append("NPCMenuBridge missing OpenNPCDialog")

          if 'local MENU_ATTRIBUTES = {' not in text["bridge"] or 'clearMenus(player)' not in text["bridge"]:
              problems.append("NPCMenuBridge must centralize and clear menu attributes")
          if 'local MENU_ATTRIBUTES = {' not in text["touch"] or 'player:SetAttribute(attribute, nil)' not in text["touch"]:
              problems.append("TouchMenus must centralize and clear menu attributes")
          if 'local MENU_ATTRIBUTES = {' not in text["dialog"] or 'clearMenuState()' not in text["dialog"]:
              problems.append("NPCDialogMenu must centralize and clear menu attributes")

          if 'local function openDialog(player, npcId, character)' not in text["bridge"]:
              problems.append("NPCMenuBridge must bind deferred dialog opening to the triggering Character")
          if 'player.Character == character and character.Parent' not in text["bridge"]:
              problems.append("NPCMenuBridge deferred open must reject stale Character callbacks")
          if 'openDialog(player, model.Name, character)' not in text["bridge"]:
              problems.append("NPCMenuBridge prompt handler must pass the triggering Character into deferred dialog state")
          if 'local playerCharacterConnections = setmetatable({}, { __mode = "k" })' not in text["bridge"]:
              problems.append("NPCMenuBridge must keep Character connection state weak-keyed")
          if 'local function bindPlayer(player)' not in text["bridge"] or 'playerCharacterConnections[player] = player.CharacterAdded:Connect' not in text["bridge"]:
              problems.append("NPCMenuBridge must bind CharacterAdded for lifecycle cleanup")
          if 'playerCharacterConnections[player] = player.CharacterAdded:Connect(function(character)' not in text["bridge"]:
              problems.append("NPCMenuBridge CharacterAdded connection must be explicit")
          if 'if player.Parent and player.Character == character then' not in text["bridge"] or 'clearMenus(player)' not in text["bridge"]:
              problems.append("NPCMenuBridge must clear menu state on the current respawn Character")
          if 'disconnectPlayer(player)' not in text["bridge"] or 'Players.PlayerRemoving:Connect(function(player)' not in text["bridge"]:
              problems.append("NPCMenuBridge must disconnect per-player lifecycle state on leave")

          menu_guards = {
              "quest": "OpenQuestMenu",
              "shop": "OpenShopMenu",
              "crystal": "OpenCrystalMenu",
              "craft": "OpenCraftingMenu",
              "achievement": "OpenAchievementMenu",
          }
          for name, attribute in menu_guards.items():
              if f'player:GetAttributeChangedSignal("{attribute}")' not in text[name]:
                  problems.append(f"{name} menu must listen for {attribute}")
              if f'player:GetAttribute("{attribute}") == nil then return end' not in text[name]:
                  problems.append(f"{name} menu must not open when {attribute} is cleared")
              if f'player:SetAttribute("{attribute}", nil)' not in text[name]:
                  problems.append(f"{name} menu must clear {attribute} on close")

          if problems:
              for problem in problems:
                  print(f" - {problem}")
              raise SystemExit(1)
          print("Menu attribute, Character-bound deferred-open, respawn cleanup and close-state contract validated.")
